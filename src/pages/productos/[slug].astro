---
export const prerender = true;

Astro.response.headers.set("Cache-Control", "public, max-age=60");

import { getProducts, getProductBySlug } from "@server/api/products";
import { getPromotionSlides } from "@server/api/promotions";
import Layout from "@layouts/Layout.astro";
import BreadcrumbSchema from '@components/seo/BreadcrumbSchema.astro';
import Breadcrumb from '@components/navigation/Breadcrumb.astro';
import ProductSlider from '@components/sections/ProductSlider.astro';
import FichaTecnica from '@components/product/TechnicalSheet.astro';
import ShareButtons from '@components/product/ShareButtons.astro';
import GalleryPopup from '@components/product/GalleryPopUp.astro';
import PopUpSolicitarCotizacion from '@components/popup/popUpSolicitarCotizacion.astro';
import ButtonPDPSticky from "@components/buttonPDPSticky.astro";
import ImageResponsive from "@components/media/ImageResponsive.astro";
import Newsletter from '@components/seo/Newsletter.astro';
import EspecialidadesSlider from "@components/sections/EspecialidadesSlider.astro";
import PromotionSlider from "@components/sections/PromotionSlider.astro";
import { getHomeEspecialidadesForSlider } from "@lib/homeEspecialidades";
import { isUsableBrandSlug } from "@lib/brandSlug";
import ProductVariants from '@components/product/ProductVariants.astro';
import { getLocaleMeta, localizePath } from "@lib/locale";
import { absoluteOgImage } from "@lib/og";
import TestimonialsSlider from '@components/sections/TestimonialsSlider.astro';

export async function getStaticPaths() {
  const products = await getProducts();

  return products.map((product) => ({
    params: { slug: product.slug },
  }));
}

const { slug } = Astro.params;

if (!slug) {
  throw new Error("Missing product slug");
}

const product = await getProductBySlug(slug);

if (!product) {
  return Astro.redirect("/404");
}

const especialidadesSliderItems = getHomeEspecialidadesForSlider();
const promotionsData = await getPromotionSlides();

const baseUrl = Astro.site ? new URL(Astro.site).origin : "https://www.medrent.mx";
const currentPathname = Astro.originPathname ?? Astro.url.pathname;
const localeMeta = getLocaleMeta(currentPathname);
const canonicalUrl = new URL(currentPathname, Astro.site).toString();
const breadcrumbId = `${canonicalUrl}#breadcrumb`;
const heroImageUrl =
  Array.isArray(product.gallery) && product.gallery.length > 0
    ? product.gallery[0]
    : undefined;
const resolvedProductImage = absoluteOgImage(heroImageUrl, baseUrl);
const pdpTitle = `${product.name} | Equipo médico especializado | MedRent`;
const pdpDescription =
  (typeof product.description === "string" && product.description.trim()) ||
  `${product.name} | Equipo médico especializado de MedRent. Referencia ${slug}.`;

const productId = `${baseUrl}/entity/product/${product.slug}#id`;
const offerId = `${baseUrl}/entity/offer/${product.slug}#offer`;
const homeUrl = new URL(localizePath("/", currentPathname), Astro.site).toString();
const productsUrl = new URL(localizePath("/productos", currentPathname), Astro.site).toString();
const brandPath = product.brand?.slug
  ? localizePath(`/nuestras-marcas/${product.brand.slug}`, currentPathname)
  : "";
const brandUrl = brandPath ? new URL(brandPath, Astro.site).toString() : "";
const item_variant  = product?.relatedProducts?.length + ' Canales, ' + product?.variants?.length + ' Derivaciones';
const firstVariant = Array.isArray((product as any)?.variants)
  ? (product as any).variants[0]
  : undefined;
const ratingValue =
  firstVariant?.ratingValue ??
  firstVariant?.rating ??
  firstVariant?.aggregateRating?.ratingValue;
const reviewCount =
  firstVariant?.reviewCount ??
  firstVariant?.reviews ??
  firstVariant?.aggregateRating?.reviewCount;

const pdpGraph: any[] = [
  {
    "@type": "WebPage",
    "@id": `${canonicalUrl}#webpage`,
    url: canonicalUrl,
    name: pdpTitle,
    description: pdpDescription,
    isPartOf: { "@id": `${baseUrl}/#website` },
    about: { "@id": `${baseUrl}/#organization` },
    inLanguage: localeMeta.htmlLang,
    breadcrumb: { "@id": breadcrumbId },
    mainEntity: { "@id": productId },
    primaryImageOfPage: {
      "@type": "ImageObject",
      url: resolvedProductImage,
    },
  },
  {
    "@type": ["Product", "MedicalDevice"],
    "@id": productId,
    name: product.name,
    description: pdpDescription,
    image: [resolvedProductImage],
    category: ["Medical Device"],
    brand:
      product.brand && isUsableBrandSlug(product.brand.slug)
        ? { "@type": "Brand", "@id": `${brandUrl}#brand` }
        : undefined,
    datePublished: product.createdAt,
    offers: { "@id": offerId },
  },
  {
    "@type": "Offer",
    "@id": offerId,
    url: canonicalUrl,
    availability: "https://schema.org/InStock",
    description: "Cotizacion disponible bajo solicitud a traves de contacto comercial.",
    businessFunction: "https://schema.org/Sell",
    eligibleCustomerType: "https://schema.org/BusinessCustomer",
    seller: { "@id": `${baseUrl}/#organization` },
  },
];

if (ratingValue && reviewCount) {
  pdpGraph.push({
    "@type": "AggregateRating",
    "@id": `${productId}#aggregateRating`,
    ratingValue,
    reviewCount,
  });
}

const pdpJsonLd = {
  "@context": "https://schema.org",
  "@graph": pdpGraph,
};

---
<Layout
  title={pdpTitle}
  description={pdpDescription}
  keywords={`${product.name}; equipo médico; MedRent`}
  ogType="product"
  pageType="product"
  ogImage={heroImageUrl}
>
  <Fragment slot="head">
    <BreadcrumbSchema
      items={[
        { name: 'Inicio', url: homeUrl },
        { name: 'Productos', url: productsUrl },
        { name: product.name, url: canonicalUrl },
      ]}
      id={breadcrumbId}
    />
    <script type="application/ld+json" set:html={JSON.stringify(pdpJsonLd)}></script>
  </Fragment>
  <Breadcrumb
    textColor="text-black"
    items={[
      { name: "INICIO", href: localizePath("/", currentPathname) },
      { name: "PRODUCTOS", href: localizePath("/productos", currentPathname) },
      { name: product.name.toUpperCase() }
    ]}
  />
  
  <!-- Contenido Principal del Producto -->
  <main class="max-w-7xl mx-auto px-4 py-8">
    <section class="max-w-7xl mx-auto py-6">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-12">
        <!-- =========================
          GALERÍA
        ========================== -->
        <!-- Galería de imágenes mobile tipo Slide (solo mobile) -->
        <div class="flex flex-col lg:flex-row-reverse gap-4 lg:sticky lg:top-32 self-start items-center lg:items-start sm:hidden">
          <div class="w-full relative mt-12">
            {product.isNew && (
              <div class="badge-launch-mobile">
                LANZAMIENTO
                <span class="badge-launch-mobile-tail" aria-hidden="true"></span>
              </div>
            )}
            <div id="glider-slideimagenesProducto" class="w-full px-0 mobile-gallery-slider overflow-x-auto scroll-smooth" data-lenis-prevent data-gallery-images={JSON.stringify(product.gallery)}>
              <ul class="flex snap-x snap-mandatory">
                {product.gallery && product.gallery.map((item, index) => {
                  const isImage = /\.(jpg|jpeg|png|webp|gif|svg)$/i.test(item);
                  const isVideo = /\.(mp4|webm|ogg)$/i.test(item);
                  return (
                    <li class="w-full shrink-0 snap-start px-0">
                      <div class="w-full aspect-square rounded-[16px] bg-white overflow-hidden relative">
                        {isImage ? (
                          <ImageResponsive
                            src={item}
                            alt={product.name || "Imagen del producto"}
                            class="w-full h-full object-contain transition-all duration-500 cursor-pointer"
                            loading={index === 0 ? "eager" : "lazy"}
                            decoding="async"
                            fetchpriority={index === 0 ? "high" : "auto"}
                            id="main-image-mobile"
                            sizes="100vw"
                          />
                        ) : isVideo ? (
                          <video
                            src={item || '/images/Productos/default.webp'}
                            controls
                            class="w-full h-full object-contain transition-all duration-500 cursor-pointer"
                            id="main-image-mobile"
                            data-type="video"
                          />
                        ) : null}
                      </div>
                    </li>
                  );
                })}
              </ul>
            </div>
          </div>

          <div id="mediaButtonsMobile" class="flex flex-row items-center gap-2 left-0 bottom-0 w-full z-20 px-2 py-2 justify-between bg-transparent relative">
            <!-- Contenedor btns share -->
            <ShareButtons classNames="flex flex-row items-center gap-2 w-full justify-between" />

            <div class="absolute left-1/2 -translate-x-1/2 bottom-3 z-30 pointer-events-none">
              <span
                id="mobile-gallery-counter"
                class="inline-flex items-center justify-center gap-2 rounded-full border border-[var(--color-text-default)] bg-[#F3FFFD] px-4 py-1 text-center text-[var(--color-text-default)] text-[16px] font-normal leading-6 tracking-[0.5px]"
              >
                1 / {product?.gallery?.length ?? 0}
              </span>
            </div>

            <button
              type="button"
              data-zoom-button
              aria-pressed="false"
              title="Activar zoom"
              class="w-10 h-10 flex items-center justify-center rounded cursor-pointer border border-[#212121] bg-transparent text-[#212121] transition-colors duration-200"
            >
                <svg class="zoom-icon-inactive w-6 h-6" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M14.9999 9.9999H11.9999V6.9999C11.9999 6.73469 11.8945 6.48033 11.707 6.2928C11.5195 6.10526 11.2651 5.9999 10.9999 5.9999C10.7347 5.9999 10.4803 6.10526 10.2928 6.2928C10.1053 6.48033 9.9999 6.73469 9.9999 6.9999V9.9999H6.9999C6.73469 9.9999 6.48033 10.1053 6.2928 10.2928C6.10526 10.4803 5.9999 10.7347 5.9999 10.9999C5.9999 11.2651 6.10526 11.5195 6.2928 11.707C6.48033 11.8945 6.73469 11.9999 6.9999 11.9999H9.9999V14.9999C9.9999 15.2651 10.1053 15.5195 10.2928 15.707C10.4803 15.8945 10.7347 15.9999 10.9999 15.9999C11.2651 15.9999 11.5195 15.8945 11.707 15.707C11.8945 15.5195 11.9999 15.2651 11.9999 14.9999V11.9999H14.9999C15.2651 11.9999 15.5195 11.8945 15.707 11.707C15.8945 11.5195 15.9999 11.2651 15.9999 10.9999C15.9999 10.7347 15.8945 10.4803 15.707 10.2928C15.5195 10.1053 15.2651 9.9999 14.9999 9.9999ZM21.7099 20.2899L17.9999 16.6099C19.44 14.8143 20.1374 12.5352 19.9487 10.2412C19.76 7.94721 18.6996 5.81269 16.9854 4.27655C15.2713 2.74041 13.0337 1.91941 10.7328 1.98237C8.43194 2.04534 6.24263 2.98747 4.61505 4.61505C2.98747 6.24263 2.04534 8.43194 1.98237 10.7328C1.91941 13.0337 2.74041 15.2713 4.27655 16.9854C5.81269 18.6996 7.94721 19.76 10.2412 19.9487C12.5352 20.1374 14.8143 19.44 16.6099 17.9999L20.2899 21.6799C20.3829 21.7736 20.4935 21.848 20.6153 21.8988C20.7372 21.9496 20.8679 21.9757 20.9999 21.9757C21.1319 21.9757 21.2626 21.9496 21.3845 21.8988C21.5063 21.848 21.6169 21.7736 21.7099 21.6799C21.8901 21.4934 21.9909 21.2442 21.9909 20.9849C21.9909 20.7256 21.8901 20.4764 21.7099 20.2899ZM10.9999 17.9999C9.61544 17.9999 8.26206 17.5894 7.11091 16.8202C5.95977 16.051 5.06256 14.9578 4.53275 13.6787C4.00293 12.3996 3.86431 10.9921 4.13441 9.63427C4.4045 8.27641 5.07119 7.02912 6.05016 6.05016C7.02912 5.07119 8.27641 4.4045 9.63427 4.13441C10.9921 3.86431 12.3996 4.00293 13.6787 4.53275C14.9578 5.06256 16.051 5.95977 16.8202 7.11091C17.5894 8.26206 17.9999 9.61544 17.9999 10.9999C17.9999 12.8564 17.2624 14.6369 15.9497 15.9497C14.6369 17.2624 12.8564 17.9999 10.9999 17.9999Z" fill="#212121"/>
                </svg>
                <svg class="zoom-icon-active hidden w-6 h-6" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M21.71 20.29L18 16.61C19.4401 14.8144 20.1375 12.5353 19.9488 10.2413C19.7601 7.94733 18.6997 5.81281 16.9855 4.27667C15.2714 2.74053 13.0338 1.91954 10.7329 1.9825C8.43207 2.04546 6.24275 2.98759 4.61517 4.61517C2.98759 6.24275 2.04546 8.43207 1.9825 10.7329C1.91954 13.0338 2.74053 15.2714 4.27667 16.9855C5.81281 18.6997 7.94733 19.7601 10.2413 19.9488C12.5353 20.1375 14.8144 19.4401 16.61 18L20.29 21.68C20.383 21.7738 20.4936 21.8482 20.6154 21.8989C20.7373 21.9497 20.868 21.9758 21 21.9758C21.132 21.9758 21.2627 21.9497 21.3846 21.8989C21.5065 21.8482 21.6171 21.7738 21.71 21.68C21.8903 21.4936 21.991 21.2444 21.991 20.985C21.991 20.7257 21.8903 20.4765 21.71 20.29ZM11 18C9.61556 18 8.26218 17.5895 7.11103 16.8203C5.95989 16.0511 5.06268 14.9579 4.53287 13.6788C4.00306 12.3997 3.86443 10.9923 4.13453 9.63439C4.40463 8.27653 5.07131 7.02925 6.05028 6.05028C7.02925 5.07131 8.27653 4.40463 9.63439 4.13453C10.9923 3.86443 12.3997 4.00306 13.6788 4.53287C14.9579 5.06268 16.0511 5.95989 16.8203 7.11103C17.5895 8.26218 18 9.61556 18 11C18 12.8565 17.2625 14.637 15.9498 15.9498C14.637 17.2625 12.8565 18 11 18ZM15 10H7.00003C6.73481 10 6.48046 10.1054 6.29292 10.2929C6.10538 10.4805 6.00003 10.7348 6.00003 11C6.00003 11.2652 6.10538 11.5196 6.29292 11.7071C6.48046 11.8947 6.73481 12 7.00003 12H15C15.2652 12 15.5196 11.8947 15.7071 11.7071C15.8947 11.5196 16 11.2652 16 11C16 10.7348 15.8947 10.4805 15.7071 10.2929C15.5196 10.1054 15.2652 10 15 10Z" fill="white"/>
                </svg>
            </button>
          </div>
        </div>

        <!-- Galería de Imágenes (solo tablet/desktop) -->
        <div id="galleryProducto" class="flex flex-col lg:flex-row-reverse gap-4 lg:sticky lg:top-32 self-start items-center lg:items-start hidden sm:flex mt-8">

          <!-- Imagen Principal -->  
          <div class="flex-1 aspect-squar bg-white rounded-lg overflow-hidden relative">
            {product.isNew && (
              <div class="absolute top-3 left-3 bg-[var(--color-primary)] text-white text-[11px] font-semibold px-3 py-1 rounded-[2px] z-20 w-fit">
                LANZAMIENTO
                <span class="absolute -bottom-2 right-2 w-0 h-0 border-l-[8px] border-l-transparent border-t-[8px] border-t-[var(--color-primary)]"></span>
              </div>    
            )}
            <div id="main-media-container" style="min-height: 400px; justify-content: center; display: flex; align-items: center; margin-bottom: 16px;">
              {product.gallery.length > 0 ? (
                <ImageResponsive
                  src={product.gallery[0]}
                  alt={product.name}
                  class="w-full h-full object-contain max-h-[600px] rounded-lg"
                  id="main-image-desktop"
                  loading="eager"
                  decoding="async"
                  fetchpriority="high"
                  sizes="(min-width: 1024px) 50vw, 100vw"
                />
              ) : (
                <div class="text-gray-400">Sin imagen</div>
              )}
            </div>

            <div id="mediaButtons" class="flex flex-row items-center gap-2 left-0 bottom-0 w-full z-20 px-2 py-2 justify-between bg-transparent">
              <!-- Contenedor btns share -->
              <ShareButtons classNames="flex flex-row items-center gap-2 w-full justify-between" />
                
              <div class="w-10 h-10">
                <button
                  type="button"
                  data-zoom-button
                  aria-pressed="false"
                  title="Activar zoom"
                  class="w-10 h-10 flex absolute z-10 items-center justify-center rounded cursor-pointer border border-[#212121] bg-transparent text-[#212121] transition-colors duration-200"
                >
                  <svg class="zoom-icon-inactive w-6 h-6" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M14.9999 9.9999H11.9999V6.9999C11.9999 6.73469 11.8945 6.48033 11.707 6.2928C11.5195 6.10526 11.2651 5.9999 10.9999 5.9999C10.7347 5.9999 10.4803 6.10526 10.2928 6.2928C10.1053 6.48033 9.9999 6.73469 9.9999 6.9999V9.9999H6.9999C6.73469 9.9999 6.48033 10.1053 6.2928 10.2928C6.10526 10.4803 5.9999 10.7347 5.9999 10.9999C5.9999 11.2651 6.10526 11.5195 6.2928 11.707C6.48033 11.8945 6.73469 11.9999 6.9999 11.9999H9.9999V14.9999C9.9999 15.2651 10.1053 15.5195 10.2928 15.707C10.4803 15.8945 10.7347 15.9999 10.9999 15.9999C11.2651 15.9999 11.5195 15.8945 11.707 15.707C11.8945 15.5195 11.9999 15.2651 11.9999 14.9999V11.9999H14.9999C15.2651 11.9999 15.5195 11.8945 15.707 11.707C15.8945 11.5195 15.9999 11.2651 15.9999 10.9999C15.9999 10.7347 15.8945 10.4803 15.707 10.2928C15.5195 10.1053 15.2651 9.9999 14.9999 9.9999ZM21.7099 20.2899L17.9999 16.6099C19.44 14.8143 20.1374 12.5352 19.9487 10.2412C19.76 7.94721 18.6996 5.81269 16.9854 4.27655C15.2713 2.74041 13.0337 1.91941 10.7328 1.98237C8.43194 2.04534 6.24263 2.98747 4.61505 4.61505C2.98747 6.24263 2.04534 8.43194 1.98237 10.7328C1.91941 13.0337 2.74041 15.2713 4.27655 16.9854C5.81269 18.6996 7.94721 19.76 10.2412 19.9487C12.5352 20.1374 14.8143 19.44 16.6099 17.9999L20.2899 21.6799C20.3829 21.7736 20.4935 21.848 20.6153 21.8988C20.7372 21.9496 20.8679 21.9757 20.9999 21.9757C21.1319 21.9757 21.2626 21.9496 21.3845 21.8988C21.5063 21.848 21.6169 21.7736 21.7099 21.6799C21.8901 21.4934 21.9909 21.2442 21.9909 20.9849C21.9909 20.7256 21.8901 20.4764 21.7099 20.2899ZM10.9999 17.9999C9.61544 17.9999 8.26206 17.5894 7.11091 16.8202C5.95977 16.051 5.06256 14.9578 4.53275 13.6787C4.00293 12.3996 3.86431 10.9921 4.13441 9.63427C4.4045 8.27641 5.07119 7.02912 6.05016 6.05016C7.02912 5.07119 8.27641 4.4045 9.63427 4.13441C10.9921 3.86431 12.3996 4.00293 13.6787 4.53275C14.9578 5.06256 16.051 5.95977 16.8202 7.11091C17.5894 8.26206 17.9999 9.61544 17.9999 10.9999C17.9999 12.8564 17.2624 14.6369 15.9497 15.9497C14.6369 17.2624 12.8564 17.9999 10.9999 17.9999Z" fill="#212121"/>
                  </svg>
                  <svg class="zoom-icon-active hidden w-6 h-6" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M21.71 20.29L18 16.61C19.4401 14.8144 20.1375 12.5353 19.9488 10.2413C19.7601 7.94733 18.6997 5.81281 16.9855 4.27667C15.2714 2.74053 13.0338 1.91954 10.7329 1.9825C8.43207 2.04546 6.24275 2.98759 4.61517 4.61517C2.98759 6.24275 2.04546 8.43207 1.9825 10.7329C1.91954 13.0338 2.74053 15.2714 4.27667 16.9855C5.81281 18.6997 7.94733 19.7601 10.2413 19.9488C12.5353 20.1375 14.8144 19.4401 16.61 18L20.29 21.68C20.383 21.7738 20.4936 21.8482 20.6154 21.8989C20.7373 21.9497 20.868 21.9758 21 21.9758C21.132 21.9758 21.2627 21.9497 21.3846 21.8989C21.5065 21.8482 21.6171 21.7738 21.71 21.68C21.8903 21.4936 21.991 21.2444 21.991 20.985C21.991 20.7257 21.8903 20.4765 21.71 20.29ZM11 18C9.61556 18 8.26218 17.5895 7.11103 16.8203C5.95989 16.0511 5.06268 14.9579 4.53287 13.6788C4.00306 12.3997 3.86443 10.9923 4.13453 9.63439C4.40463 8.27653 5.07131 7.02925 6.05028 6.05028C7.02925 5.07131 8.27653 4.40463 9.63439 4.13453C10.9923 3.86443 12.3997 4.00306 13.6788 4.53287C14.9579 5.06268 16.0511 5.95989 16.8203 7.11103C17.5895 8.26218 18 9.61556 18 11C18 12.8565 17.2625 14.637 15.9498 15.9498C14.637 17.2625 12.8565 18 11 18ZM15 10H7.00003C6.73481 10 6.48046 10.1054 6.29292 10.2929C6.10538 10.4805 6.00003 10.7348 6.00003 11C6.00003 11.2652 6.10538 11.5196 6.29292 11.7071C6.48046 11.8947 6.73481 12 7.00003 12H15C15.2652 12 15.5196 11.8947 15.7071 11.7071C15.8947 11.5196 16 11.2652 16 11C16 10.7348 15.8947 10.4805 15.7071 10.2929C15.5196 10.1054 15.2652 10 15 10Z" fill="white"/>
                  </svg>
                </button>
              </div>
            </div>
          </div>

          <!-- Miniaturas debajo de la imagen principal (tablet/desktop: 6 máx, overlay en la última) -->
          {product?.gallery && product.gallery.length > 1 && (
            <div class="relative w-full lg:w-24 flex-shrink-0 hidden sm:block py-6 lg:py-0">
                <div class="flex flex-row lg:flex-col gap-3 w-full lg:w-24 max-h-[420px] lg:max-h-fit overflow-y-auto justify-center" id="thumbnails-scroll" style="scrollbar-width: none; -ms-overflow-style: none; justify-content: center;">
                <style>
                    #thumbnails-scroll::-webkit-scrollbar { display: none; }
                </style>
                {(() => {
                  const total = product.gallery.length;
                  const maxThumbnails = 6;
                    return product.gallery.slice(0, maxThumbnails).map((imagen, index) => {
                      const isImage = /\.(jpg|jpeg|png|webp|gif|svg)$/i.test(imagen);
                      const isVideo = /\.(mp4|webm|ogg)$/i.test(imagen);
                      // Si es la última visible y hay más, muestra el overlay '+N'
                      if (index === maxThumbnails - 1 && total > maxThumbnails) {
                      return (
                        <div class="relative w-20 h-20 btnVerPopUpImagenes" data-imagenes={JSON.stringify(product.gallery)}>
                          <button
                            class="w-20 h-20 bg-white rounded overflow-hidden transition-colors cursor-pointer border border-[var(--color-secondary-light)] hover:border-[var(--color-primary)]"
                            style="min-height:80px; max-height:80px;"
                            data-gallery-popup="true"
                            data-start-index={index}
                            type="button"
                          >
                            {isImage ? (
                              <ImageResponsive
                                src={imagen}
                                alt={`Miniatura ${index + 1}`}
                                class="w-full h-full object-cover opacity-20"
                                loading="lazy"
                                decoding="async"
                                sizes="80px"
                              />
                            ) : isVideo ? (
                              <video src={imagen} class="w-full h-full object-cover opacity-20" />
                            ) : null}
                            <div class="absolute inset-0 bg-opacity-60 flex items-center justify-center text-[var(--color-primary)] text-lg font-bold rounded">
                              +{total - maxThumbnails + 1}
                            </div>
                          </button>
                        </div>
                      );
                    }
                      return (
                        <div class="relative w-20 h-20 " data-gallery={JSON.stringify(product.gallery)}>
                          <button
                            class="w-20 h-20 bg-white rounded overflow-hidden transition-colors cursor-pointer border border-[var(--color-secondary-light)] hover:border-[var(--color-primary)]"
                            style="min-height:80px; max-height:80px;"
                            data-thumbnail={imagen}
                            data-type={isImage ? 'image' : isVideo ? 'video' : 'other'}
                            data-index={index}
                            type="button"
                          >
                            {isImage ? (
                              <ImageResponsive
                                src={imagen}
                                alt={`Miniatura ${index + 1}`}
                                class="w-full h-full object-cover"
                                loading="lazy"
                                decoding="async"
                                sizes="80px"
                              />
                            ) : isVideo ? (
                              <video src={imagen} class="w-full h-full object-cover" />
                            ) : null}
                          </button>
                        </div>
                      );
                    });
                })()}
                {/* GalleryPopup debe estar fuera del mapeo para que window.openGalleryPopup funcione siempre */}
                {/* {Array.isArray(product?.gallery) && product.gallery.length > 6 && (
                    <GalleryPopup gallery={product.gallery} startIndex={6} />
                )} */}

                </div>
            </div>
          )}
        </div>

        <GalleryPopup gallery={product.gallery} />

        <!-- INFO PRODUCTO -->
        <div class="space-y-6 lg:px-6 gap-2 flex flex-col ">

          {product.brand && isUsableBrandSlug(product.brand.slug) && (
            <a
              href={localizePath(`/nuestras-marcas/${product.brand.slug}`, currentPathname)}
              class="uppercase text-[var(--color-primary)] hover:underline text-sm m-0"
            >
              {product.brand.name}
            </a>
          )}
          {product.brand && !isUsableBrandSlug(product.brand.slug) && product.brand.name && (
            <span class="uppercase text-[var(--color-primary)] text-sm m-0">{product.brand.name}</span>
          )}

          <h1 class="self-stretch m-0 uppercase font-bold text-[28px] leading-[36px] text-[var(--color-green-dark)] md:text-3xl md:leading-[1.2]">
            {product.name}
          </h1>

          {product.description && (
            <p class="text-lg text-[var(--color-text-default)] leading-relaxed">
              {product.description}
            </p>
          )}

          <!-- Acciones -->
          <div class="flex flex-col sm:flex-row gap-4 m-0">
            <button 
                id="open-event-popup"
                class="w-full sm:w-1/2 text-white rounded px-4 py-4  text-sm font-bold bg-[var(--color-primary)] hover:bg-[var(--color-accent)] transition-colors cursor-pointer border-none flex justify-center items-center gap-2"
            >
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="16" viewBox="0 0 20 16" fill="none">
                    <path d="M2 16C1.45 16 0.975 15.8083 0.575 15.425C0.191667 15.025 0 14.55 0 14V2C0 1.45 0.191667 0.983334 0.575 0.599999C0.975 0.2 1.45 0 2 0H18C18.55 0 19.0167 0.2 19.4 0.599999C19.8 0.983334 20 1.45 20 2V14C20 14.55 19.8 15.025 19.4 15.425C19.0167 15.8083 18.55 16 18 16H2ZM10 9L18 4V2L10 7L2 2V4L10 9Z" fill="white"/>
                </svg>
                Solicitar cotización
            </button>
            <a 
                id="open-demo-whatsapp"
                class="w-full sm:w-1/2 flex justify-center items-center rounded border border-[var(--color-primary)] bg-[var(--color-secondary-light)] px-4 py-3 gap-2 text-[var(--color-primary)]  text-[14px] font-bold text-center hover:bg-[var(--color-accent)] transition-colors cursor-pointer hover:text-white group"
                href="https://wa.me/525559854347" target="_blank" rel="noopener noreferrer" style="gap: 8px;"
            >
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path class="transition-colors group-hover:fill-white" d="M10 0C15.523 0 20 4.477 20 10C20 15.523 15.523 20 10 20C8.23277 20.003 6.4966 19.5353 4.97002 18.645L0.00401546 20L1.35602 15.032C0.464974 13.5049 -0.00308163 11.768 1.52687e-05 10C1.52687e-05 4.477 4.47702 0 10 0ZM6.59202 5.3L6.39201 5.308C6.26271 5.31691 6.13636 5.35087 6.02002 5.408C5.91159 5.46951 5.81258 5.5463 5.72602 5.636C5.60602 5.749 5.53802 5.847 5.46502 5.942C5.09514 6.4229 4.89599 7.01331 4.89902 7.62C4.90102 8.11 5.02902 8.587 5.22902 9.033C5.63802 9.935 6.31102 10.89 7.19902 11.775C7.41302 11.988 7.62302 12.202 7.84902 12.401C8.95245 13.3724 10.2673 14.073 11.689 14.447L12.257 14.534C12.442 14.544 12.627 14.53 12.813 14.521C13.1042 14.5056 13.3885 14.4268 13.646 14.29C13.7769 14.2223 13.9047 14.1489 14.029 14.07C14.029 14.07 14.0714 14.0413 14.154 13.98C14.289 13.88 14.372 13.809 14.484 13.692C14.568 13.6053 14.638 13.5047 14.694 13.39C14.772 13.227 14.85 12.916 14.882 12.657C14.906 12.459 14.899 12.351 14.896 12.284C14.892 12.177 14.803 12.066 14.706 12.019L14.124 11.758C14.124 11.758 13.254 11.379 12.722 11.137C12.6663 11.1128 12.6067 11.0989 12.546 11.096C12.4776 11.0888 12.4084 11.0965 12.3432 11.1184C12.278 11.1403 12.2182 11.176 12.168 11.223C12.163 11.221 12.096 11.278 11.373 12.154C11.3315 12.2098 11.2744 12.2519 11.2088 12.2751C11.1433 12.2982 11.0723 12.3013 11.005 12.284C10.9398 12.2666 10.876 12.2446 10.814 12.218C10.69 12.166 10.647 12.146 10.562 12.11C9.98789 11.8599 9.45646 11.5215 8.98702 11.107C8.86102 10.997 8.74402 10.877 8.62402 10.761C8.23063 10.3842 7.88777 9.95799 7.60401 9.493L7.54501 9.398C7.50328 9.3338 7.46905 9.26501 7.44302 9.193C7.40502 9.046 7.50402 8.928 7.50402 8.928C7.50402 8.928 7.74702 8.662 7.86002 8.518C7.97002 8.378 8.06301 8.242 8.12302 8.145C8.24102 7.955 8.27802 7.76 8.21602 7.609C7.93602 6.925 7.64668 6.24467 7.34802 5.568C7.28902 5.434 7.11402 5.338 6.95502 5.319C6.90102 5.31233 6.84701 5.307 6.79302 5.303C6.65874 5.2953 6.52411 5.29664 6.39002 5.307L6.59202 5.3Z" fill="#006764"/>
                </svg>
                Agenda tu DEMO
            </a>
          </div>

          <!-- Beneficios -->
          {product?.benefit && product.benefit.length > 0 && (
            <div class="p-6 pb-0 space-y-6 mb-0">
                {product.benefit.map((benefit) => (
                <div class="rounded-lg flex items-start gap-4">
                    {/* Icono */}
                    {benefit.icon?.url && (
                    <div class="flex-shrink-0  rounded-full bg-[#E3FFFC] p-4">
                        <img
                        src={benefit.icon.url}
                        alt={benefit.benefit}
                        class="w-6 h-6 object-contain"
                        />
                    </div>
                    )}
                    {/* Contenido */}
                    <div class="flex-1">
                    <h3 class="font-lato text-[16px] font-bold text-[var(--color-primary)]">
                      {benefit.benefit}
                    </h3>
                    <p class="font-lato text-[16px] font-normal text-[var(--color-green-dark)]">
                      {benefit.description}
                    </p>
                    </div>
                </div>
                ))}
            </div>
          )}

          <ProductVariants currentProductSlug={slug} />

          <!-- Preguntas Frecuentes -->
          {product?.faqItem && product.faqItem.length > 0 && (
            <div id="preguntas-frecuentes" class="pt-6">
              <h3 class="pdp-section-title">
                Preguntas frecuentes
              </h3>
            
              <div class="space-y-4" x-data="{ openItems: [] }">
                {product.faqItem.map((faq, index) => {
                  const itemIndex = index + 1;
                  return (
                    <div class="group px-2 py-4 relative overflow-hidden m-0" style="border-bottom: 1px solid var(--color-primary);">
                      <div class="faq-hover-fill-effect"></div>
                      <div class="relative z-10">
                          <div class="flex items-center justify-between gap-4">
                              <div 
                                  class="cursor-pointer flex-1" 
                                  x-on:click={`openItems.includes(${itemIndex}) ? openItems = openItems.filter(item => item !== ${itemIndex}) : openItems.push(${itemIndex})`}
                              >
                                  <h4 style="font-family: Lato; font-size: 16px; font-style: normal; color: #261819;" class="cursor-pointer font-bold ">
                                      {itemIndex}. {faq.question}
                                  </h4>
                              </div>
                              <button
                                  aria-label="Abrir" 
                                  x-on:click.stop={`openItems.includes(${itemIndex}) ? openItems = openItems.filter(item => item !== ${itemIndex}) : openItems.push(${itemIndex})`}
                                  :class={`openItems.includes(${itemIndex}) ? 'bg-transparent text-[var(--color-primary)]' : 'bg-transparent text-[var(--color-primary)]'`}
                                  class="rounded px-3 py-2 flex items-center justify-center cursor-pointer transition-all duration-300 border-none flex-shrink-0"
                              >
                                  <span 
                                      class="inline-block transform transition-transform duration-300"
                                      :class={`openItems.includes(${itemIndex}) ? '' : 'rotate-45'`}
                                  >
                                      ✕
                                  </span>
                              </button>
                          </div>
                          
                          <div 
                              x-ref={`content${itemIndex}`}
                              x-bind:style={`openItems.includes(${itemIndex}) ? 'max-height:' + $refs['content' + ${itemIndex}].scrollHeight + 'px' : 'max-height: 0px'`}
                              class="transition-all duration-500 ease-in-out overflow-hidden"
                          >
                              <p style="font-family: Lato; font-size: 16px; font-style: italic;" class="mt-4 pl-6 text-on-surface-variant">
                                  {faq.answer}
                              </p>
                          </div>
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          )}
        </div>
      </div>

      <!-- Consumibles -->
      <ProductSlider
        data={product?.relatedConsumables} 
        title="Consumibles" 
        prefixId="Consumibles"
        disableHorizontalPadding={true}
      />

      <FichaTecnica data={product?.technicalSheet} archivoPDF={product?.documents} />
      
      <!-- Especificacion Técnica -->
      {product?.technicalSpecifications && product.technicalSpecifications.length > 0 && (
        <section class="max-w-7xl mx-auto py-8">
          <div class="flex flex-col" x-data="{ openTechItems: [] }">
            <h2 class="pdp-section-title">
              Especificaciones técnicas
            </h2>
            
            {product.technicalSpecifications.map((item, index) => {
              const itemIndex = index + 1;
              const isFirstItem = index === 0;
              
              return (
                <div 
                  class="group py-4 px-2 relative overflow-hidden rounded-[2px]" 
                  style="border-bottom: 0.5px solid var(--color-primary);"
                >
                  <div 
                    class="absolute inset-0 bg-[#EDF2F0] transform translate-y-full cursor-pointer md:group-hover:translate-y-0 transition-transform duration-300 ease-out rounded-[2px] hover:text-white"
                    x-bind:class={`{'translate-y-0 bg-white': openTechItems.includes(${itemIndex})}`}
                  ></div>
                  
                  <div class="relative z-10">
                    <div class="flex items-start md:items-center justify-between gap-4">
                      <div 
                        class="flex items-start gap-2 cursor-pointer flex-1" 
                        x-on:click={`openTechItems.includes(${itemIndex}) ? openTechItems = openTechItems.filter(i => i !== ${itemIndex}) : openTechItems.push(${itemIndex})`}
                      >
                        <span 
                          class="font-light text-[16px] pt-[2px] transition-colors duration-300 text-[var(--color-primary)]"
                        >
                          {String(itemIndex).padStart(2, '0')}
                        </span>
                        <h2 
                          class="text-[16px] font-bold transition-colors duration-300"
                        >
                          {item.title}
                        </h2>
                      </div>
                      <button
                        aria-label="Abrir" 
                        x-on:click={`openTechItems.includes(${itemIndex}) ? openTechItems = openTechItems.filter(i => i !== ${itemIndex}) : openTechItems.push(${itemIndex})`}
                        x-bind:class={`{'text-[var(--color-primary)] bg-[var(--color-secondary-light)]': openTechItems.includes(${itemIndex}), 'bg-transparent text-[var(--color-primary)]': !openTechItems.includes(${itemIndex})}`}
                        class="rounded px-3 py-2 flex items-center justify-center cursor-pointer transition-colors duration-300 border-none relative z-20"
                      >
                        <span 
                          class="inline-block transform transition-transform duration-300 text-[20px] font-light"
                          x-bind:class={`{'': openTechItems.includes(${itemIndex}), 'rotate-45': !openTechItems.includes(${itemIndex})}`}
                        >
                          ✕
                        </span>
                      </button>
                    </div>
                    
                    <div 
                      x-ref={`techContent${itemIndex}`}
                      x-bind:style={`openTechItems.includes(${itemIndex}) ? 'max-height: ' + $refs['techContent${itemIndex}'].scrollHeight + 'px' : 'max-height: 0px'`}
                      class="transition-all duration-500 ease-in-out overflow-hidden"
                    >
                      <div class="mt-4 px-2">
                        <div class="flex flex-col md:flex-row gap-6 items-stretch md:items-start">
                          {/* Imagen del producto - SOLO para el primer ítem */}
                          {isFirstItem && product.gallery && product.gallery.length > 0 && (
                            <div class="w-full md:w-[160px] md:flex-shrink-0 flex items-center justify-center relative z-10">
                              <div class="w-full aspect-square md:aspect-[4/3]">
                                <img
                                  src={product.gallery[0]}
                                  alt={product.name}
                                  class="w-full h-full object-contain rounded-[16px] bg-white"
                                />
                              </div>
                            </div>
                          )}
                          
                          {/* Contenido de la especificación */}
                          <div class="flex-1">
                            {/* Descripción */}
                            <p 
                              class="text-[16px] font-medium mb-4 transition-colors duration-300 text-on-surface-variant"
                            >
                              {item.description}
                            </p>
                            
                            {/* Accesorios relacionados */}
                            {item.relatedAccessories && item.relatedAccessories.length > 0 && (
                              <div class="mt-4">
                                <h5 
                                  class="font-bold text-[16px] mb-2 transition-colors duration-300"
                                >
                                  Accesorios relacionados:
                                </h5>
                                <ul class="list-disc list-inside space-y-1">
                                  {item.relatedAccessories.map((accesorio) => (
                                    <li 
                                      class="text-[16px] font-medium transition-colors duration-300"
                                      style="font-family: Lato;"
                                    >
                                      {accesorio.accessory}
                                    </li>
                                  ))}
                                </ul>
                              </div>
                            )}
                            
                            {/* Imagen específica de la especificación (si existe) */}
                            {item.gallerypecificacion?.url && (
                              <div class="mt-4 flex items-center justify-center md:justify-start">
                                <div class="w-full aspect-square md:w-[160px] md:aspect-[4/3] md:flex-shrink-0">
                                  <img
                                    src={buildImageUrl(item.gallerypecificacion.url)}
                                    alt={item.title}
                                    class="w-full h-full object-contain rounded-[16px] bg-white"
                                  />
                                </div>
                              </div>
                            )}
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              );
            })}
          </div>
        </section>
      )}

      <!-- Btn sticky -->
      <ButtonPDPSticky
        Images={product.gallery}
        product={product.name}
        brand={product.brand?.name}
      />
      
      <!-- Consumibles -->
      <ProductSlider
        data={product?.relatedProducts} 
        title="Productos relacionados" 
        prefixId="ProductosRelacionados"
        disableHorizontalPadding={true}
      />

      <!-- Slider de promociones -->
      <PromotionSlider
        promotionsData={promotionsData}
        disableHorizontalPadding={true}
        pdpSectionTitle={true}
      />

      <EspecialidadesSlider items={especialidadesSliderItems} prefixId="PDPEspecialidades" />

      
    </section>

  </main>
      
  <!-- Newsletter -->
  <Newsletter />

  <!-- Testimonios -->
  <TestimonialsSlider productId={product.id} />

  </main>

  <script is:inline define:vars={{ product, item_variant  }}>
    /* Event: view_item_list */
    setTimeout(() => {
      window.view_item(product, item_variant)
    }, 2000);
  </script>

<!-- BTN Compartir url -->
<script is:inline>
  // Botón Copiar URL (compartir)
  document.addEventListener('DOMContentLoaded', function() {
      // Desktop
      const copyBtnDesktop = document.getElementById('copy-url-btn');
      if (copyBtnDesktop) {
          copyBtnDesktop.addEventListener('click', async function(e) {
              e.preventDefault();
              try {
                  await navigator.clipboard.writeText(window.location.href);
                  const prevBg = copyBtnDesktop.style.backgroundColor;
                  copyBtnDesktop.style.backgroundColor = '#B60D45';
                  setTimeout(() => { copyBtnDesktop.style.backgroundColor = prevBg; }, 500);
              } catch (err) {
                  alert('No se pudo copiar la URL');
              }
          });
      }
      // Mobile
      const copyBtnMobile = document.getElementById('copy-url-btn-mobile');
      if (copyBtnMobile) {
          copyBtnMobile.addEventListener('click', async function(e) {
              e.preventDefault();
              try {
                  await navigator.clipboard.writeText(window.location.href);
                  const prevBg = copyBtnMobile.style.backgroundColor;
                  copyBtnMobile.style.backgroundColor = '#B60D45';
                  setTimeout(() => { copyBtnMobile.style.backgroundColor = prevBg; }, 500);
              } catch (err) {
                  alert('No se pudo copiar la URL');
              }
          });
      }
  });
</script>

<!-- BTN SHARE -->
<script>
    document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('.relative').forEach(container => {
      const toggleBtn = container.querySelector('.share-toggle');
      const closeBtn = container.querySelector('.share-close');
      const actions = container.querySelector('.share-actions');
      const tooltip = container.querySelector('.copy-tooltip');
      const copyBtn = container.querySelector('.copy-url');
      
      const mediaButtons = container.closest('#mediaButtonsMobile, #mediaButtons');
      const galleryCounter = mediaButtons?.querySelector('#mobile-gallery-counter');
      
      if (!toggleBtn) return;
      
      const url = encodeURIComponent(window.location.href);
      
      const facebook = container.querySelector('.share-facebook');
      const whatsapp = container.querySelector('.share-whatsapp');
      const twitter = container.querySelector('.share-twitter');
      const linkedin = container.querySelector('.share-linkedin');
      
      if (facebook) facebook.href = `https://www.facebook.com/sharer/sharer.php?u=${url}`;
      if (whatsapp) whatsapp.href = `https://wa.me/?text=${url}`;
      if (twitter) twitter.href = `https://twitter.com/intent/tweet?url=${url}`;
      if (linkedin) linkedin.href = `https://www.linkedin.com/shareArticle?mini=true&url=${url}`;
      
      // Estado inicial: actions ocultos y NO clickeables
      actions.style.pointerEvents = 'none';
      
      toggleBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        toggleBtn.classList.add('opacity-0', 'scale-90', 'pointer-events-none');
        actions.classList.remove('opacity-0', 'scale-95');
        actions.classList.add('opacity-100', 'scale-100');
        actions.style.pointerEvents = 'auto'; // Permitir clicks cuando está visible
        if (galleryCounter) galleryCounter.style.opacity = '0';
      });
      
      closeBtn?.addEventListener('click', (e) => {
        e.stopPropagation();
        toggleBtn.classList.remove('opacity-0', 'scale-90', 'pointer-events-none');
        actions.classList.add('opacity-0', 'scale-95');
        actions.classList.remove('opacity-100', 'scale-100');
        actions.style.pointerEvents = 'none'; // Deshabilitar clicks cuando está oculto
        if (galleryCounter) galleryCounter.style.opacity = '1';
      });
      
      copyBtn?.addEventListener('click', async () => {
        await navigator.clipboard.writeText(window.location.href);
        tooltip.classList.remove('opacity-0');
        tooltip.classList.add('opacity-100');
        setTimeout(() => {
          tooltip.classList.add('opacity-0');
          tooltip.classList.remove('opacity-100');
        }, 1500);
      });
    });
  });
</script>

<!-- BTN ZOOM -->
<script is:inline>
  let zoomActive = false;
  let currentMainImage = null;

  function getZoomButtons() {
    return Array.from(document.querySelectorAll('[data-zoom-button]'));
  }

  function updateZoomButtonState(button, active) {
    button.setAttribute('aria-pressed', String(active));
    button.classList.toggle('bg-[var(--color-primary)]', active);
    button.classList.toggle('bg-transparent', !active);
    button.classList.toggle('text-white', active);
    button.classList.toggle('text-[#212121]', !active);

    const activeIcon = button.querySelector('.zoom-icon-active');
    const inactiveIcon = button.querySelector('.zoom-icon-inactive');
    if (activeIcon) activeIcon.classList.toggle('hidden', !active);
    if (inactiveIcon) inactiveIcon.classList.toggle('hidden', active);
  }

  function isTouchGalleryScreen() {
    return window.matchMedia('(max-width: 1023px)').matches;
  }

  function getMobileGalleryImages() {
    const slider = document.getElementById('glider-slideimagenesProducto');
    if (!slider) return [];
    try {
      return JSON.parse(slider.dataset.galleryImages || '[]');
    } catch (error) {
      return [];
    }
  }

  function getCurrentMobileImageIndex() {
    const slider = document.getElementById('glider-slideimagenesProducto');
    const images = getMobileGalleryImages();
    if (!slider || !images.length) return 0;
    const width = slider.clientWidth || 1;
    const index = Math.round(slider.scrollLeft / width);
    return Math.min(Math.max(index, 0), images.length - 1);
  }

  function initZoom() {
    const zoomButtons = getZoomButtons();
    const mainImage = document.getElementById('main-image-desktop');
    if (!zoomButtons.length) return;

    currentMainImage?.removeEventListener('mousemove', handleMouseMove);
    currentMainImage?.removeEventListener('mouseleave', handleMouseLeave);

    if (mainImage) {
      currentMainImage = mainImage;
      currentMainImage.style.cursor = zoomActive ? 'zoom-in' : 'default';
      currentMainImage.style.transform = 'scale(1)';
      currentMainImage.style.transformOrigin = 'center';
      currentMainImage.style.transition = 'transform 0.2s ease';
    } else {
      currentMainImage = null;
    }

    zoomButtons.forEach((button) => {
      button.removeEventListener('click', handleZoomClick);
      button.addEventListener('click', handleZoomClick);
      updateZoomButtonState(button, zoomActive);
    });

    if (currentMainImage) {
      currentMainImage.addEventListener('mousemove', handleMouseMove);
      currentMainImage.addEventListener('mouseleave', handleMouseLeave);
    }
  }

  function handleZoomClick(event) {
    event.preventDefault();

    if (isTouchGalleryScreen()) {
      const images = getMobileGalleryImages();
      const index = getCurrentMobileImageIndex();
      if (Array.isArray(images) && images.length && typeof window.openGalleryPopup === 'function') {
        window.openGalleryPopup(images, index);
      }
      return;
    }

    zoomActive = !zoomActive;
    currentMainImage.style.cursor = zoomActive ? 'zoom-in' : 'default';
    if (!zoomActive) {
      currentMainImage.style.transform = 'scale(1)';
      currentMainImage.style.transformOrigin = 'center';
    }

    getZoomButtons().forEach((button) => updateZoomButtonState(button, zoomActive));
  }

  function handleMouseMove(event) {
    if (!zoomActive || !currentMainImage) return;
    const rect = currentMainImage.getBoundingClientRect();
    const x = ((event.clientX - rect.left) / rect.width) * 100;
    const y = ((event.clientY - rect.top) / rect.height) * 100;
    currentMainImage.style.transformOrigin = `${x}% ${y}%`;
    currentMainImage.style.transform = 'scale(2)';
  }

  function handleMouseLeave() {
    if (zoomActive && currentMainImage) {
      currentMainImage.style.transform = 'scale(1)';
    }
  }

  document.addEventListener('DOMContentLoaded', initZoom);
  window.reinitZoom = initZoom;
</script>

<!-- Slider mobile -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Slider mobile
    const slider = document.getElementById('glider-slideimagenesProducto');
    const counter = document.getElementById('mobile-gallery-counter');
    if (slider && counter) {
      const slides = slider.querySelectorAll('li');
      const total = slides.length;
      if (total) {
        let rafId = 0, snapTimer = 0;
        const updateCounter = () => {
          const width = slider.clientWidth || 1;
          const index = Math.min(total - 1, Math.max(0, Math.round(slider.scrollLeft / width)));
          counter.textContent = `${index + 1} / ${total}`;
          return { width, index };
        };
        const onScroll = () => {
          if (rafId) return;
          rafId = requestAnimationFrame(() => {
            const { width, index } = updateCounter();
            if (snapTimer) clearTimeout(snapTimer);
            snapTimer = setTimeout(() => slider.scrollTo({ left: index * width, behavior: 'smooth' }), 80);
            rafId = 0;
          });
        };
        slider.addEventListener('scroll', onScroll, { passive: true });
        window.addEventListener('resize', updateCounter);
        updateCounter();
      }
    }

    // Miniaturas desktop - cambiar imagen principal
    const thumbnails = document.querySelectorAll('[data-thumbnail]:not([data-gallery-popup])');
    const mainMediaContainer = document.getElementById('main-media-container');
    
    thumbnails.forEach(thumb => {
      thumb.addEventListener('click', function() {
        const newSrc = this.dataset.thumbnail;
        const type = this.dataset.type;
        if (mainMediaContainer && newSrc) {
          if (type === 'image') {
            mainMediaContainer.innerHTML = `<img src='${newSrc}' alt='' class='w-full h-full object-contain max-h-[600px] rounded-lg' id='main-image-desktop' data-type='image' />`;
          } else if (type === 'video') {
            mainMediaContainer.innerHTML = `<video src='${newSrc}' controls class='w-full h-full object-contain max-h-[600px] rounded-lg' id='main-image-desktop' data-type='video'></video>`;
          }
          if (window.reinitZoom) window.reinitZoom();
          
          document.querySelectorAll('[data-thumbnail]').forEach(t => {
            t.className = 'w-20 h-20 bg-white rounded overflow-hidden transition-colors cursor-pointer border border-[var(--color-secondary-light)] hover:border-[var(--color-primary)]';
          });
          this.className = 'w-20 h-20 bg-white rounded overflow-hidden transition-colors cursor-pointer border-3 border-[var(--color-primary)]';
        }
      });
    });

    // Popup galería
    const popupBtn = document.querySelector('[data-gallery-popup="true"]');
    if (popupBtn) {
      popupBtn.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        const container = this.closest('.btnVerPopUpImagenes');
        const images = container?.dataset.imagenes ? JSON.parse(container.dataset.imagenes) : [];
        if (window.openGalleryPopup) {
          window.openGalleryPopup(images, 5);
        }
      });
    }
  });
</script>

<!-- Pop up -->
<script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    const openBtn = document.getElementById('open-event-popup');
    
    if (openBtn) {
      openBtn.addEventListener('click', function(e) {
        e.preventDefault();
        // Disparar un evento personalizado que escuche el popup
        document.dispatchEvent(new CustomEvent('open-event-popup'));
      });
    }
  });
</script>

<PopUpSolicitarCotizacion 
  imagesForHero={product.gallery || []}
  productData={product}
  aplicacionData={product.aplicacion}
  categoriaData={product.categoria}
/>
</Layout>

<style>
  .faq-hover-fill-effect {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: #EDF2F0;
    transform: scaleY(0);
    transform-origin: bottom center;
    transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
    z-index: 1;
    border-radius: 2px;
  }

  .badge-launch-mobile {
    position: absolute;
    left: 16px;
    top: 16px;
    z-index: 30;
    display: inline-flex;
    padding: 2px 4px;
    justify-content: center;
    align-items: center;
    border-radius: 2px;
    background: #006191;
    color: #fff;
    text-align: center;
    font-family: Lato, sans-serif;
    font-size: 14px;
    font-style: normal;
    font-weight: 500;
    line-height: 1.43;
    letter-spacing: 0.1px;
  }

  .badge-launch-mobile-tail {
    position: absolute;
    right: 8px;
    bottom: -8px;
    width: 0;
    height: 0;
    border-left: 8px solid transparent;
    border-top: 8px solid #006191;
  }

  .relative.z-10 {
    position: relative;
    z-index: 10;
  }

  @media (min-width: 768px) {
    .group:hover .faq-hover-fill-effect {
      transform: scaleY(1);
    }
  }
</style>
